{% extends 'back/base.html.twig' %}

{% block title %}Liste des Pays{% endblock %}

{% block body %}


<h1> Liste des Pays</h1>


<br>

<div class="d-grid gap-2 col-4 mx-auto">
 <button type="button" class="btn btn-outline-dark"><a href="{{ path('admin_country_new') }}">Créer un nouveau Pays</a></button>
</div>

<br>

<div class="container">
<div class="row row-cols-2 row-cols-lg-6 g-2 g-lg-2">

    {% for country in countries %}
  <div class="btn btn-outline-dark"><li class="list-group-item"><a href="{{ path('admin_country_show', {id : country.id}) }}">{{ country.name }}</a></li></div>
    {% endfor %}
     
   </div>
</div>

<br>
 
<a href="{{ path('homepage') }}">Back to homepage</a>

{% endblock %}
