<form method="post" action="{{ path('admin_video_room_delete', {'id': video_room.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ video_room.id) }}">
    <button class="btn">Delete</button>
</form>
