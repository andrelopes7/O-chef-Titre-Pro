{% extends 'front/base.html.twig' %}

{% block title %}Blog{% endblock %}

{% block body %}

    <div class="card bg-dark text-white">
       <img src="../images/back/oldelpachot-5l-Q8p_feHE-unsplash.jpg" class="img-fluid" alt="gastronomy dish">
      <div class="card-img-overlay">
      
        <h1 class="card-title" style="color: white; font-style: italic; font-family: 'Times New Roman', Times, serif; text-align: right; font-size: 5em;"><strong>Le Blog </strong></h1>
      </div>
        <div class="input-group mb-1">
          <button class="btn btn-outline-warning" type="button" id="button-addon1" style="width: 15em;">Rechercher</button>
          <input type="text" class="form-control" placeholder="" aria-label="Example text with button addon" aria-describedby="button-addon1">
        </div>
    </div>

<div class="form_new_show">
    <h1>{{ blog.title }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <td>{{ blog.content }}</td>
            </tr>
            <tr>
                <th>Medias</th>
                <td>{{ blog.media }}</td>
            </tr>
        </tbody>
    </table>
</div>
    <a href="{{ path('blog_index') }}">Retour</a>

    <a href="{{ path('blog_edit', {'id': blog.id}) }}">edit</a>

    {{ include('front/blog/_delete_form.html.twig') }}
{% endblock %}
