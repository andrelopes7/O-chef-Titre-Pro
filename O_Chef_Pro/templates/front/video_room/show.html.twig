{% extends 'front/base.html.twig' %}

{% block title %}VideoRoom{% endblock %}

{% block body %}


    <div class="card bg-dark text-white">
       <img src="../images/back/teemu-paananen-bzdhc5b3Bxs-unsplash.jpg" class="img-fluid" alt="gastronomy dish">
      <div class="card-img-overlay">
      
        <h1 class="card-title" style="color: white; font-style: italic; font-family: 'Times New Roman', Times, serif; text-align: right; font-size: 5em;"><strong>Vidéo Room</strong></h1>
        <h2 class="card-text" style="color: white; font-style: italic; font-family: 'Times New Roman', Times, serif; text-align: right;"><strong>Fait par des chefs, pour des chefs</strong></h2>
      </div>
        <div class="input-group mb-1">
          <button class="btn btn-outline-warning" type="button" id="button-addon1" style="width: 30em;">Rechercher</button>
          <input type="text" class="form-control" placeholder="" aria-label="Example text with button addon" aria-describedby="button-addon1">
        </div>
    </div>

    <h1>VideoRoom</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ video_room.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ video_room.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ video_room.description }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ video_room.link }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ video_room.createdAt ? video_room.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ video_room.updatedAt ? video_room.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('video_room_index') }}">back to list</a>

{% endblock %}
